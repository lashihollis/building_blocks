A focused pipeline that:
------------------------

    1.Pulls real FHIR data from a public API
    2.Saves it as CSV files (no database setup needed)
    3. Loads into DuckDB and feeds into the analytics engineering project dbt pipeline


